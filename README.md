# Telegram Bot для уведомлений о записях Dikidi

Бот для автоматической отправки уведомлений клиентам о записях в салоне через систему Dikidi.

## Возможности

- ✅ Регистрация по номеру телефона
- 📅 Автоматический парсинг записей с сайта Dikidi
- 🔔 6 типов уведомлений:
  1. Создание записи
  2. Изменение записи
  3. Отмена записи
  4. Сообщение после визита
  5. Напоминание за несколько часов до записи
  6. Напоминание за 14 дней до записи

## Технологии

- Python 3.10+
- aiogram 3 (Telegram Bot API)
- PostgreSQL (база данных)
- SQLAlchemy (ORM)
- APScheduler (планировщик задач)
- Playwright (парсинг веб-страниц)

## Установка

### 1. Клонирование репозитория

```bash
git clone <repository_url>
cd MeowNoMeow
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Установка браузеров для Playwright

```bash
playwright install chromium
```

### 4. Настройка базы данных PostgreSQL

Создайте базу данных:

```sql
CREATE DATABASE dikidi_bot;
```

### 5. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните необходимые переменные:

```env
# Telegram Bot
BOT_TOKEN=your_bot_token_here

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/dikidi_bot

# Dikidi
DIKIDI_COMPANY_ID=1993359
DIKIDI_JOURNAL_URL=https://dikidi.ru/ru/owner/journal/?company=1993359
DIKIDI_LOGIN_PHONE=89526834874
DIKIDI_LOGIN_PASSWORD=281076zxc

# Admin
ADMIN_TELEGRAM_ID=your_admin_telegram_id
```

### 6. Инициализация базы данных

```bash
python init_db.py
```

## Запуск

```bash
python main.py
```

## Структура проекта

```
MeowNoMeow/
├── main.py                # Главный файл запуска бота
├── init_db.py              # Скрипт инициализации БД
├── bot/                    # Основной пакет бота
│   ├── __init__.py
│   ├── config.py           # Конфигурация
│   ├── database/           # Работа с базой данных
│   │   ├── __init__.py
│   │   ├── database.py     # Настройка подключения к БД
│   │   └── database_init.py # Инициализация БД
│   ├── models/             # Модели базы данных
│   │   ├── __init__.py
│   │   └── models.py       # SQLAlchemy модели
│   ├── handlers/           # Обработчики команд бота
│   │   ├── __init__.py
│   │   └── handlers.py     # Обработчики сообщений
│   └── services/          # Сервисы бота
│       ├── __init__.py
│       ├── dikidi_parser.py    # Парсер записей с Dikidi
│       ├── notifications.py    # Сервис уведомлений
│       └── scheduler.py        # Планировщик задач
├── requirements.txt       # Зависимости
├── .env                   # Переменные окружения (создать)
├── PARSER_SETUP.md        # Инструкция по настройке парсера
└── README.md              # Документация
```

## База данных

### Таблицы

- **companies** - информация о салонах
- **users** - пользователи бота (связь Telegram ID и номера телефона)
- **appointments** - записи клиентов
- **notifications** - запланированные уведомления

## Команды бота

- `/start` - Начать работу с ботом (регистрация по номеру телефона)
- `/my_appointments` - Показать мои записи
- `/help` - Справка по командам

## Настройка парсера Dikidi

Парсер автоматически выполняет авторизацию на сайте Dikidi и парсит записи из календаря.

**Процесс работы парсера:**
1. Переходит на главную страницу https://dikidi.ru/
2. Нажимает кнопку "Вход/регистрация"
3. Выбирает вход по номеру телефона
4. Вводит номер телефона и пароль (из `.env`)
5. Переходит на страницу журнала записей
6. Парсит все записи из календаря

**Парсируемые данные:**
- Мастер
- Дата
- Время
- Услуга
- Клиент (имя)
- Номер телефона клиента

**Тестирование парсера:**
```bash
python test_parser.py
```

Подробнее см. `PARSER_SETUP.md`

## Расписание задач

- **Синхронизация записей**: каждые 5 минут
- **Обработка уведомлений**: каждую минуту

## Примечания

1. Для работы парсера необходим доступ к странице журнала Dikidi (возможно, потребуется авторизация)
2. Номера телефонов нормализуются к формату `+7XXXXXXXXXX`
3. Уведомления отправляются только зарегистрированным пользователям (сопоставление по номеру телефона)

## Лицензия

MIT
